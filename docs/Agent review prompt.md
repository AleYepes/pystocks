Read docs/implementation_roadmap.md and the 4 documents it indicates. You'll find that we've mostly completed phases 1-3, with some of phase 6 CLI work to test the pipeline. Before we continue to building phase 4, I'd like to investigate the state of the implementation so far, and verify that it's shaping up into a coherent and organized repo, as was intended by the documents indicated in docs/implementation_roadmap.md .

I recently attempted to refactor my first MVP `old/` into a more architecturally sound version `pystocks/`, and then `pystocks_next/`. Both versions improved greatly on the original notebook based pipeline. The first `pystocks/` refined the notebook logic into more production-grade scripts, but it was built without architecture in mind, and as it grew, it became much more coupled. The second was to rebuild on a clearer architecture, that way the code may be more maintainable, legible, and scalable over time. This implied an affinity for simplicity, removing redundancies and overly defensive-code, "cutting the fat" to keep a clear order of operations, however, it managed to sacrifice the statistical veracity and quality of the ingestion (fetching far fewer endpoints, and cutting corners in the time-series regressions). They did add very interesting panel creation that wasn't in the original, so both definitely improve on their predecesors, but at the same time, they both added so much overhead that I can no longer understand the logic or architecture of the repo. I'm considering starting over, once again, but this time manually assessing the architectural plans and implementation, one de-coupled module at a time, instead of letting loose and having you or other agents take the wheel completely. 

I want you to read the ingestion, storage, and preprocessing modules in `pystocks/` and then the universe, collection, storage, and feature_inputs modules in `pystocks_next/`. This should give you a broad understanding of the first part of the pipeline. You will then grill-me as to how we should build the next implementation from scratch. I ultimately will want to migrate the snapshots from an old db generated by the current `pystocks/` next, so keep that in mind. I will also need to carefully implement the new version one step at a time, not all at once.

The fundamental goal behind the pystocks_next refactoring was to rebuild on a clearer architecture, that way the code may be more maintainable, legible, and scalable over time. This implied an affinity for simplicity, removing redundancies and "cutting the fat" to keep a clear order of operations.

Do you see any architectural issues with pystocks_next/ that we should address before testing and debugging the pipeline so far